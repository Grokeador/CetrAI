Pacchetto di valutazione di AIDefender per Windows 10/11 a 64 bit.

Chi ha una versione da 0.1.5248 in poi non deve fare niente: la riceve da solo entro 24 ore.

**Cosa cambia in ogni versione sta nelle note della release**, non qui:
[releases/latest](../../releases/latest).

Questo file teneva una seconda copia di quelle note, e come ogni seconda copia è invecchiata senza
che nessuno se ne accorgesse: dichiarava 715 test automatici mentre il prodotto ne aveva già molti di
più, e raccontava una modifica di tre versioni prima come se fosse l'ultima. Un numero che nessuno
ricontrolla è peggio di nessun numero.
